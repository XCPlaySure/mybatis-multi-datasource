<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xchan.example.datasource1.mapper.ExampleMapper1">
    <select id="mainQuery" resultType="HashMap">
        select *
        from eka.lst_user
        where lde_id = '01'
        and lus_active = 1
        fetch next 10 rows only
    </select>
</mapper>